SUPABASE_URL=
SUPABASE_KEY=

FRONTEND_URL=http://localhost:5173

# Optional -- client-request invite emails are skipped (logged only) if unset.
SMTP_HOST=
SMTP_PORT=587
SMTP_USER=
SMTP_PASSWORD=
SMTP_FROM=
